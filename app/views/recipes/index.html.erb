<h1>Recipes</h1>
<ul>
<% @recipes.each do |recipe|%>
  <li><%= link_to recipe.name, recipe_path(recipe) %> <%= link_to "edit", edit_recipe_path(recipe)%> <%= link_to "delete", recipe_path(recipe), method: :delete%> </li>
<% end %>
</ul>

<%= link_to "New Recipe", new_recipe_path %>