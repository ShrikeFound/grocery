<h1>These are the Ingredients</h1>

<ul>
<% @ingredients.each do |i|%>
  <li><%= i.name %> <%= i.pretty_price%> <%= link_to "edit", edit_ingredient_path(i)  %> <%= link_to "delete", ingredient_path(i), method: :delete  %></li>
<% end %>
</ul>


<%= link_to "New Ingredient", new_ingredient_path%>